apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "celery-worker-heavy.fullname" . }}-test-connection"
  labels:
    {{- include "celery-worker-heavy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "celery-worker-heavy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
