{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "celery-worker-heavy.fullname" . }}
  labels:
    {{- include "celery-worker-heavy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "celery-worker-heavy.selectorLabels" . | nindent 4 }}
{{- end }}